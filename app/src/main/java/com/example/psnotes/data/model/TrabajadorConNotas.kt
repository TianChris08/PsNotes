package com.example.psnotes.data.model

import androidx.room.Embedded
import androidx.room.Relation

/*data class TrabajadorConNotas(
    @Embedded val trabajador: Trabajador,
    @Relation(
        parentColumn = "id",
        entityColumn = "trabajadorId"
    )
    val notas: List<Nota>
)
*/